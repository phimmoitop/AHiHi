name: M3U8 Download

on:
  workflow_dispatch:
    inputs:
      link:
        description: 'Link'
        required: true
      slug:
        description: 'Slug'
        required: true
      token:
        description: 'Token'
        required: true
      page:
        description: 'Pages'
        required: true
      account:
        description: 'Account'
        required: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg aria2 curl jq
          python3 -m pip install -U yt-dlp

      - name: Download + Verify M3U8 Video
        shell: bash
        run: |
          set -e

          URL="${{ github.event.inputs.link }}"

          DOMAINS=(
            "thunderleaf12.online"
            "aurorabird6.live"
            "rainorbit33.xyz"
            "frostcomet5.pro"
            "nightbreeze17.site"
          )

          PREFIX="https://limit.fatherlessdad.workers.dev"

          echo "Getting real m3u8..."
          M3U8=$(yt-dlp -g "$URL")

          echo "Downloading playlist..."
          curl -sL "$M3U8" -o playlist.m3u8

          grep -v '^#' playlist.m3u8 | grep -v '^[[:space:]]*$' > segments.txt
          TOTAL=$(wc -l < segments.txt)

          echo "Total segments: $TOTAL"

          mkdir -p segments
          mapfile -t SEGS < segments.txt

          download_segment() {
            IDX=$1
            SEG="${SEGS[$IDX]}"
            FILE=$(printf "segments/seg_%05d.ts" "$IDX")
            
            ATTEMPT=0
            MAX_RETRIES=20

            while [ "$ATTEMPT" -lt "$MAX_RETRIES" ]; do
              DOMAIN=${DOMAINS[$((ATTEMPT % ${#DOMAINS[@]}))]}
              SEG_PATH=$(echo "$SEG" | sed -E 's|^/?[^/]+/|/|')
              FULL="$PREFIX/$DOMAIN$SEG_PATH"

              if (( ATTEMPT >= 5 )); then
                RAND=$RANDOM
                FULL="$FULL?v=$RAND"
              fi

              /usr/bin/aria2c \
                --quiet=true \
                --allow-overwrite=true \
                --file-allocation=none \
                --max-tries=1 \
                -o "$FILE.tmp" \
                "$FULL" || true

              if [ ! -f "$FILE.tmp" ]; then
                sleep 5
                ATTEMPT=$((ATTEMPT+1))
                echo "[!] Segment $IDX tải lỗi mạng (Thử lại $ATTEMPT/$MAX_RETRIES)..."
                continue
              fi

              ffprobe -v error "$FILE.tmp" >/dev/null 2>&1 || {
                rm -f "$FILE.tmp"
                sleep 5
                ATTEMPT=$((ATTEMPT+1))
                echo "[!] Segment $IDX file hỏng (Thử lại $ATTEMPT/$MAX_RETRIES)..."
                continue
              }

              mv "$FILE.tmp" "$FILE"
              return 0
            done
            
            if [ "$IDX" -eq "$((TOTAL - 1))" ]; then
              echo "[SKIP] CẢNH BÁO: Segment cuối cùng ($IDX) thất bại sau $MAX_RETRIES lần. Bỏ qua..."
              touch "segments/seg_$(printf "%05d" "$IDX").skipped"
              return 0
            else
              echo "[ERROR] Segment $IDX (ở giữa video) tải thất bại hoàn toàn sau $MAX_RETRIES lần!"
              touch "segments/FAILED_CRITICAL"
              return 1
            fi
          }

          echo "Starting parallel download..."

          for ((i=0;i<TOTAL;i++)); do
            download_segment "$i" &

            while [ "$(jobs -p | wc -l)" -ge 10 ]; do
              sleep 1
            done
          done

          while true; do
            if [ -f "segments/FAILED_CRITICAL" ]; then
              echo "Có segment ở giữa video không thể tải được. Hủy bỏ quá trình..."
              exit 1
            fi

            SUCCESS=$(find segments -maxdepth 1 -name "*.ts" 2>/dev/null | wc -l || echo 0)
            SKIPPED=$(find segments -maxdepth 1 -name "*.skipped" 2>/dev/null | wc -l || echo 0)
            DONE=$((SUCCESS + SKIPPED))
            
            if [ "$DONE" -eq "$TOTAL" ]; then
              echo "Hoàn tất! Đã xử lý $TOTAL segments ($SUCCESS thành công, $SKIPPED bỏ qua)."
              break
            fi

            MISSING=""
            for ((i=0; i<TOTAL; i++)); do
              FILE=$(printf "segments/seg_%05d" "$i")
              if [ ! -f "${FILE}.ts" ] && [ ! -f "${FILE}.skipped" ]; then
                MISSING="$MISSING $i"
              fi
            done
            
            MISSING_PREVIEW=$(echo "$MISSING" | awk '{for(i=1;i<=10&&i<=NF;i++) printf $i" "; if(NF>10) print "..."}')

            echo "Downloading... $DONE / $TOTAL segments"
            echo "Đang chờ / Đang lỗi ở các segments:[ $MISSING_PREVIEW ]"
            
            sleep 15
          done

          wait
          echo "All background downloads finished."

          echo "Merging video streams directly (Binary Concat)..."
          cat segments/*.ts > combined.ts
          
          echo "Converting to MP4 & Re-building audio track to fix ADTS errors..."
          # ĐIỂM QUAN TRỌNG NHẤT: -c:v copy (giữ hình) và -c:a aac (sửa tiếng)
          ffmpeg -loglevel error -y -i combined.ts -map 0:v? -map 0:a? -c:v copy -c:a aac input_video.mp4
          rm -f combined.ts

          echo "Checking duration..."
          EXPECTED=$(grep "#EXTINF" playlist.m3u8 | awk -F: '{sum+=$2} END{print int(sum)}')

          ACTUAL=$(ffprobe -v error \
            -show_entries format=duration \
            -of default=noprint_wrappers=1:nokey=1 \
            input_video.mp4 || echo "0")
          ACTUAL=${ACTUAL%.*}

          echo "Expected: $EXPECTED"
          echo "Actual: $ACTUAL"

          DIFF=$(echo "$EXPECTED $ACTUAL" | awk '{print ($1>$2)?$1-$2:$2-$1}')
          echo "Duration difference: $DIFF seconds"

          MAX_DIFF=60 
          if [ "$SKIPPED" -gt 0 ]; then
            echo "[INFO] Segment cuối bị bỏ qua."
          fi

          echo "Checking decode errors..."
          ERRORS=$(ffmpeg -v error -i input_video.mp4 -f null - 2>&1 | wc -l || true)
          echo "Decode errors: $ERRORS"

          if [ "$DIFF" -gt "$MAX_DIFF" ]; then
            echo "[LỖI NẶNG] Sai số thời lượng quá lớn ($DIFF giây). Xác nhận video bị hỏng."
            exit 1
          fi

          if [ "$ERRORS" -gt 0 ]; then
            echo "[WARNING] Phát hiện $ERRORS lỗi khung hình (Frame corruption) trong video gốc. Sẽ tự động khôi phục lúc Re-Encode!"
          fi

          echo "Video verified successfully"

          echo "Collecting video information..."
          SIZE=$(du -h input_video.mp4 | cut -f1)
          DURATION=$(ffprobe -v error -show_entries format=duration -of default=noprint_wrappers=1:nokey=1 input_video.mp4 || echo "N/A")
          BITRATE=$(ffprobe -v error -show_entries format=bit_rate -of default=noprint_wrappers=1:nokey=1 input_video.mp4 || echo "N/A")
          WIDTH=$(ffprobe -v error -select_streams v:0 -show_entries stream=width -of csv=p=0 input_video.mp4 | head -n1 || echo "N/A")
          HEIGHT=$(ffprobe -v error -select_streams v:0 -show_entries stream=height -of csv=p=0 input_video.mp4 | head -n1 || echo "N/A")
          FPS=$(ffprobe -v error -select_streams v:0 -show_entries stream=r_frame_rate -of csv=p=0 input_video.mp4 | head -n1 || echo "N/A")
          CODEC=$(ffprobe -v error -select_streams v:0 -show_entries stream=codec_name -of csv=p=0 input_video.mp4 | head -n1 || echo "N/A")

          echo "================ VIDEO INFO ================"
          echo "File: input_video.mp4"
          echo "Size: $SIZE"
          echo "Duration: $DURATION seconds"
          echo "Resolution: ${WIDTH}x${HEIGHT}"
          echo "FPS: $FPS"
          echo "Codec: $CODEC"
          echo "Bitrate: $BITRATE"
          echo "==========================================="
          
      - name: Re-Encode & Convert to HLS fMP4 (Strict 5.00s Segments as PNG)
        run: |
          mkdir -p output
          
          # 1. ĐỌC FPS GỐC CỦA VIDEO (Ví dụ: 24000/1001 hoặc 30000/1001)
          RAW_FPS=$(ffprobe -v error -select_streams v:0 -show_entries stream=r_frame_rate -of csv=p=0 input_video.mp4 | head -n1 || echo "30/1")
          
          # 2. DÙNG TOÁN HỌC ĐỂ LÀM TRÒN FPS ĐẾN SỐ NGUYÊN GẦN NHẤT
          TARGET_FPS=$(echo "$RAW_FPS" | awk -F'/' '{if($2!=0) { fps=$1/$2; printf "%.0f\n", fps } else { print 30 } }')
          
          # Đề phòng lỗi bash, mặc định sẽ là 30
          if [ -z "$TARGET_FPS" ] || [ "$TARGET_FPS" -eq 0 ]; then
            TARGET_FPS=30
          fi

          # 2. ĐỌC BITRATE VIDEO GỐC
          RAW_BITRATE=$(ffprobe -v error -select_streams v:0 -show_entries stream=bit_rate -of csv=p=0 input_video.mp4 | head -n1)
          
          # nếu bitrate không tồn tại thì fallback
          if [ -z "$RAW_BITRATE" ] || [ "$RAW_BITRATE" -eq 0 ]; then
            RAW_BITRATE=2000000
          fi
          
          # đổi sang kbps
          TARGET_BITRATE=$(( RAW_BITRATE / 1000 ))
          MAXRATE=$TARGET_BITRATE*1.2
          BUFSIZE=$(( TARGET_BITRATE * 2 ))
          
          # 3. TÍNH TOÁN ĐIỂM CẮT KEYFRAME CHO CHUẨN 5.00 GIÂY
          GOP=$(( TARGET_FPS * 5 ))
          
          echo "==========================================="
          echo "GỐC FPS (RAW): $RAW_FPS"
          echo "FPS SAU LÀM TRÒN: $TARGET_FPS fps"
          echo "KEYFRAME (GOP) MỖI 5s: $GOP frames"
          echo "==========================================="
          
          # 4. CHẠY FFMPEG VỚI CÁC THÔNG SỐ ĐỘNG ĐÃ TÍNH TOÁN
          ffmpeg -y -stats_period 30 -i input_video.mp4 \
          -map 0:v:0 -map 0:a? \
          -c:v libx264 -pix_fmt yuv420p -profile:v high -level 4.1 \
          -b:v ${TARGET_BITRATE}k \
          -maxrate ${MAXRATE}k \
          -bufsize ${BUFSIZE}k \
          -preset medium \
          -r "$TARGET_FPS" -g "$GOP" -keyint_min "$GOP" -sc_threshold 0 \
          -c:a aac -b:a 128k -ac 2 -ar 48000 \
          -threads 0 \
          -f hls \
          -start_number 10001 \
          -hls_time 5 \
          -hls_list_size 0 \
          -hls_segment_type fmp4 \
          -hls_fmp4_init_filename "${{ github.event.inputs.slug }}-index.png" \
          -hls_segment_filename "output/${{ github.event.inputs.slug }}-index%d.png" \
          "output/${{ github.event.inputs.slug }}.m3u8"
          
          # Kiểm tra xem file m3u8 có được tạo không
          if [ ! -f "output/${{ github.event.inputs.slug }}.m3u8" ]; then
            echo "FFmpeg failed to create m3u8"
            exit 1
          fi

      - name: Deploy to Cloudflare Pages (first)
        id: deploy1
        uses: cloudflare/pages-action@v1
        with:
          apiToken: ${{ github.event.inputs.token }}
          accountId: ${{ github.event.inputs.account }}
          projectName: ${{ github.event.inputs.page }}
          directory: ./output
          branch: ${{ github.event.inputs.slug }}

      - name: Verify Cloudflare deployment (branch domain check)
        id: verify
        continue-on-error: true
        env:
          PROJECT: ${{ github.event.inputs.page }}
          BRANCH: ${{ github.event.inputs.slug }}
        run: |
          set -e
          FILE_URL="https://${{ github.event.inputs.slug }}.${{ github.event.inputs.page }}.pages.dev/${{ github.event.inputs.slug }}.m3u8"

          echo "[TARGET] $FILE_URL"

          check () {
            code=$(curl -L -o /dev/null -s -w "%{http_code}" "$FILE_URL")
            echo "HTTP CODE: $code"
            [ "$code" = "200" ]
          }

          echo "[WAIT] Waiting 1 minutes for Pages propagation..."
          sleep 60

          echo "[CHECK #1]"
          if check; then
            echo "[SUCCESS] File exists"
            curl -G "https://cp.ssplay.net/vicdn.php" \
            --data-urlencode "type=ok0" \
            --data-urlencode "key=${{ github.event.inputs.page }}" \
            --data-urlencode "slug=${{ github.event.inputs.slug }}"
            exit 0
          fi

          echo "[WAIT] Still not found, wait extra 3 minutes..."
          sleep 180

          echo "[CHECK #2]"
          if check; then
            echo "[SUCCESS] File exists after delay"
            exit 0
          fi

          echo "[FAIL] File still missing → mark for redeploy"
          echo "NEED_REDEPLOY=true" >> $GITHUB_ENV
          exit 1

      - name: Deploy to Cloudflare Pages (retry – reuse encoded files)
        if: env.NEED_REDEPLOY == 'true'
        uses: cloudflare/pages-action@v1
        with:
          apiToken: ${{ github.event.inputs.token }}
          accountId: ${{ github.event.inputs.account }}
          projectName: ${{ github.event.inputs.page }}
          directory: ./output
          branch: ${{ github.event.inputs.slug }}
